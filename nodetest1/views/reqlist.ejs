<!DOCTYPE html>
<html>
<head>
    <title>Request List</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script type = "text/javascript">
        <!--
        function AutoRefresh( t ){
            setTimeout("location.reload(true);", t)
        }
        //-->
    </script>
</head>
<body onload ="JavaScript:AutoRefresh(5000);">
<h1>Request List</h1>
    <u1>
        <%
        var list = '';
        for (i = 0; i < reqlist.length; i++){
            list += '<li>ID: ' + reqlist[i]._id + ' Bed Number: ' + reqlist[i].BedNumber + ' Request Type: ' + reqlist[i].Help + ' Prio: ' + reqlist[i].Prio + ' Time Logged In: ' + reqlist[i].TimeIn +'</li>';
        }
        %>
        <%- list %>
    </u1>
    <li><a href = "personalindex">Return to Index</a></li>
</body>
</html>
